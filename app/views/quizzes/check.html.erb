<h2>Test Result</h2>
<p> You scored <%= @result%> / <%= @question_count %> </p>

<% if @failures.any? %>
<table class="table">
<thead>
  <th> Question:</th>
  <th> Your answer:</th>
  <th> Right answer: </th>
</thead>

  <% @failures.each do |fail|%>
  <tr>
    <td><%= fail[:question]%> </td>
    <td><%= fail[:user_answer]%> </td>
    <td> <%= fail[:right_answer] %> </td>
  </tr>
  <%end%>
</table>
<% end %>

<%= link_to 'Take another quiz for this unit', unit_subscription_path(@quiz.unit_subscription.id), class:"btn btn-default" %>
<%= link_to 'Back to my Dashboard', user_path(), class:"btn btn-default pull-right" %>
